[include]
    path = "~/.config/catppuccin/delta/catppuccino.gitconfig"

[commit]
    gpgsign = true

[core]
    excludesfile = ~/.config/git/ignore
    pager = delta

[diff]
    colorMoved = default

[delta]
    features = catppuccin-mocha
    navigate = true

[init]
    defaultbranch = main

[interactive]
    diffFilter = delta --color-only

[merge]
    conflictstyle = diff3

[push]
    autosetupremote = true

[rerere]
    enabled = true

[user]
    name = Samyak Bardiya
    email = <>

[includeIf "gitdir:~/.config/"]
    path = ~/.config/git/config_personal

[includeIf "gitdir:~/code/"]
    path = ~/.config/git/config_personal

[includeIf "gitdir:~/Documents/"]
    path = ~/.config/git/config_personal

[includeIf "gitdir:~/work/"]
    path = ~/.config/git/config_work

; [diff]
;   tool = nvimdiff
; [difftool]
;   prompt = false
; [difftool "nvimdiff"]
;   cmd = nvim -d "$LOCAL" "$REMOTE"

# vim:ft=gitconfig
[pull]
	rebase = false
